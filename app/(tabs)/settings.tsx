import { View, Text, Platform } from "react-native";
import { StatusBar } from "expo-status-bar";

export default function Settings() {
  return (
    <>
      <View>
        <Text>Tab [Settings|Settings]</Text>
      </View>
    </>
  );
}
